/**
    Stemming:

        The part of the process will change words like "ran" into "run".
        This will make it much easier to deal with in the long run 
        in comparrisson to trying to figure it all out
*/

// const natural = require('natural');
// const tokenizer = new natural.WordTokenizer();

// const text = "I'd like to go.";
// const tokens = tokenizer.tokenize(text);
// console.log(tokens); // Output: [ 'I', "'d", 'like', 'to', 'go', '.' ]


export default class Stemming {

}