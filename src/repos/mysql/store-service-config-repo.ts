import { BaseRepository } from '@eezze/base';
import { ERepository } from '@eezze/decorators';

@ERepository({
	datasourceType: 'Mysql',
	datasource: 'mysql-default',
	targetEntity: 'StoreServiceConfig',
})
export default class StoreServiceConfigRepository extends BaseRepository {}
